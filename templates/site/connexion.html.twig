{% extends 'style.html.twig' %}

{% block body %}

<div class="connexion">


{# {% if app.user %} #}
    {# <div class="mb-3"> #}
            {# Tu es connecté avec {{ app.user.username }}, <a href="{{ path('app_logout') }}">Logout</a> #}
        {# </div> #}
    {# {% endif %} #}

    <div class="logosite">
    <a href="{{ path('accueil') }}" class="button">
                <img src="images/logo handi'acc.png" alt="Logo du site" />
    </a>
    </div>

    <h1 class="h3 mb-3 font-weight-normal"> Se connecter </h1>

    <div class="container">

    <div class="milieu2">

    <form method="post">
    {% if error %}
        <div class="alert alert-danger">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
    {% endif %}
    
    <div class="adresseemail">
    
    <input type="email" name="email" id="inputEmail" class="form" autocomplete="email" placeholder="Email" required autofocus>

    </div>

    <div class="motdepasse">
    
    <input type="password" name="password" id="inputPassword" class="form" autocomplete="current-password" placeholder="Mot de passe" required>

    <input type="hidden" name="_csrf_token"
        value="{{ csrf_token('authenticate') }}"
    >

    

    </div>

    {#
        Uncomment this section and add a remember_me option below your firewall to activate remember me functionality.
        See https://symfony.com/doc/current/security/remember_me.html

        <div class="checkbox mb-3">
            <label>
                <input type="checkbox" name="_remember_me"> Remember me
            </label>
        </div>
    #}

    <button class="btn btn-lg btn-danger" type="submit">
    Connexion
    </button>

    </div>

    </div>

    <div class="pasdecompte">
    <p> Vous n'avez pas de compte ? </p>
    <a href="{{ path('inscription') }}"> Je m'inscris ! </a>
    </div>
</form>

</div>

{% endblock %}
