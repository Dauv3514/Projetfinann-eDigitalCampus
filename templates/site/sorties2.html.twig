{% extends 'base2.html.twig' %}

{% block body %}

<section class="sorties">

<h1> Proposition de sorties des membres </h1>

<div class="propositionsortie">
<p> Tu peux aussi proposer une sortie aux membres via ton dashboard en cliquant ici ! </p>
<a href="{{ path('dashboard') }}"> <button type="submit" class="btn btn-primary"> Proposer une sortie </button> </a> 
</div>

<div class="sortie">
<div class="alignement">


{#<div class="appel">
{% for user in users %}
<h1> {{user.prenom}} </h1>
{% endfor %}
</div> #}

<div class="sort">
{% for sortie in sorties %}
<div class="sor">
<img src="{{sortie.image}}">
<p> {{sortie.date}} </p>
<p> {{sortie.ville}}</p>
<p> {{sortie.description}} </p>
<div class="message">
<a href="{{ path('sorties') }}"> <button type="submit" class="btn btn-danger"> Envoyer un message à ... {{app.user.prenom}} </button> </a>
</div>
<div class='accepte'>
<a href="{{ path('acceptesortie', { 'id': sortie.id })}}"> <button type="submit" class="btn btn-primary"> Accepter la sortie </button> </a>
</div>
</div>
{% endfor %}
</div>
</div>





</div>




</section>

{% endblock %}

