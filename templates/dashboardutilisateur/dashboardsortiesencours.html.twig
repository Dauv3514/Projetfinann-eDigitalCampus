{% extends 'basesidebar.html.twig' %}

{% block title %}Hello SiteController!{% endblock %}

{% block body %}


<div class="e2"> 
<div class="milieu2">
<div class="boutajout">
{{app.user.email}}
<a href="{{ path('nouvellesortie')}}"> <button type="submit" class="btn btn-primary"> Ajouter une sortie </button> </a>
<h1> Mes propositions de sorties en cours (accompagnant trouvé) </h1>
</div>
</div>
</div>


<div class="sortie2">
<div class="alignement2">

{#<div class="appel2">
{% for user in users %} 
<h1> {{user.prenom}} </h1> 
{% endfor %} 
</div>#}


<div class="sort2">
{% for sortie in app.user.sorties %}
<div class="sor2">
<img src="{{sortie.image}}">
<p> {{sortie.date}} </p>
<p> {{sortie.ville}}</p>
<p> {{sortie.description}} </p>
<a href="{{ path('modificationsortie', { 'id': sortie.id })}}"> <button type="submit" class="btn btn-primary"> Modifier la sortie </button> </a>
<div class="message">
{# <a href="{{ path('sorties') }}"> <button type="submit" class="btn btn-danger"> Envoyer un message à ... {% for user in users %}{{user.pre}}{% endfor %}</button> </a> #}
</div>

<div class="poubelle">
<a href="{{path('dashboardsortiesencours')}}"> <img src="images/poubelle.png" id="sup"> </a>
</div>

</div>
{% endfor %}
</div>
</div>

<div class="nontrouve">

<h1> Mes propositions de sorties en cours (accompagnant non trouvé) </h1>

</div>

{% endblock %}